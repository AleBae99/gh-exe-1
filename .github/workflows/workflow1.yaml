name: Deploy
on: push
jobs:
  lin:
    runs-on: ubuntu-latest
    steps:
      - name: run lint
        run: npm lint
  test:
    needs: lin
    runs-on: ubuntu-latest
    steps: 
      - name: run test
        run:
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: run deploy
        run: echo "deploying..."