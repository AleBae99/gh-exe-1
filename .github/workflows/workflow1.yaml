name: Deploy
on: push
jobs:
  lin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: npm install
      - name: run lint
        run: npm lint
  test:
    needs: lin
    runs-on: ubuntu-latest
    steps: 
      - name: run test
        run: npm test
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: run deploy
        run: echo "deploying..."