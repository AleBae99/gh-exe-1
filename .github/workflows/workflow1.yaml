name: Deploy
on: push
jobs:
    lin-project:
        runs-on: ubuntu-latest
        steps:
            - name: run lint
              run: npm lint
    test-project:
        runs-on: ubuntu-latest
        steps: 
            - name: run test
              run:
    deploy-project:
        runs-on: ubuntu-latest
        steps:
            - name: run deploy
              run: echo "deploying..."